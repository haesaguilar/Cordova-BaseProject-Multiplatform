(function(){
	App.BaseView = Ember.View.extend({
		// tagName: 'div',
		// classNames: ['ember-div'],
		// templatesFolder: 'templates',
		// templatesExtension: 'hbs',
		// templateName: 'base',
		// template: null;

		// // init: function() {
		// // 	console.log('Initialize BaseView');

		// // 	var self = this;
		// // 	// get the templat
		// // 	$.get('../' + this.templatesFolder + this.templateName + this.templateExtension, function(data){
		// // 		self.template = data;
		// // 	});
		// // }
	})
})();